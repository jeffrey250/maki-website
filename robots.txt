# MĀKI — www.maki.nz
#
# Crawlers obey only the single most specific User-agent group that matches
# them, and rules are NOT inherited between groups. So every group that should
# respect the client-portal exclusion has to carry it; naming a bot in its own
# group without repeating the Disallow would exempt it. One group for everyone
# keeps that impossible to get wrong.
#
# AI crawlers (GPTBot, ClaudeBot, PerplexityBot, Applebot-Extended, Google-
# Extended, CCBot, cohere-ai and the rest) are deliberately welcome here: they
# match "*" below and are allowed the whole public site.

User-agent: *
Allow: /
Disallow: /tools/omokoroa-ponds/
Disallow: /tools/klondyke-alignment/
Disallow: /tools/klondyke-bank/
Disallow: /tools/klondyke-flow/

Sitemap: https://www.maki.nz/sitemap.xml
